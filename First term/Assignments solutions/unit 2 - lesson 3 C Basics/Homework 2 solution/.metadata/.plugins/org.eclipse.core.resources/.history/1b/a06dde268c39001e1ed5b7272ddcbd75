/*
 * main.c
 *
 *  Created on: Aug 13, 2023
 *      Author: Ahmed Melik Alnasser
 */

#include "stdio.h"

int main()
{
	char alpha_bet;

	printf("Enter an alphabet: ");
	fflush(stdout);
	scanf("%c",&alpha_bet);

	if(alphabet == ('a' || 'e' || 'i' || 'o' || 'u')
			printf("%c is a vowel",alpha_bet);
	else
		printf("%c is a consonant",alpha_bet);

	return 0;

}


